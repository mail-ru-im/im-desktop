if(APPLE)
    set(APP_PROJECT_NAME "Myteam")
elseif(UNIX)
    set(APP_PROJECT_NAME "myteam")
elseif(WIN32)
    set(APP_PROJECT_NAME "icq")
else()
    message(FATAL_ERROR "unsupported platform")
endif()